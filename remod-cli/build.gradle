description = 'The remod command-line tool: project scaffolding, building and testing mods.'

dependencies {
    api project(':remod-loader')
    api project(':remod-installer')
    api project(':remod-version-adapters')
}
